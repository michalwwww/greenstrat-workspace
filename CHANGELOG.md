# CHANGELOG — GREENSTRAT

Wszystkie znaczące zmiany w tym projekcie będą dokumentowane w tym pliku.

## [0.2.2] - 2026-08-03
### Naprawiono (B-5, B-6, B-9, B-11, B-13, N-1, N-3, N-4, N-5)
- **B-5 / B-6 (Metadata w odpowiedzi JSON)**: Dodano blok `response.metadata` (`engineVersion`, `timestamp`, `recordCount`, `acceptedCount`, `rejectedCount`, `datasetHash`) do obu akcji `getStats` i `uploadFile` w `Code.gs`.
- **B-9 (Stałe 75/40 w JASP)**: Usunięto fikcyjne wartości zastępcze `(p.eco ? 75 : 40)` i `(p.eco ? 80 : 50)` — gdy brak ocen operacyjnych `eiesi`/`eirri` = `null` (brak danych zamiast fikcji).
- **B-11 (null/100 w arkuszu Sheets)**: Zabezpieczono `ps.gpqi + "/100"` i `ps.gpqi_fin + "%"` przed `null` — wyświetlane jako `"b.d."`.
- **B-13 (Duplikaty funkcji)**: Usunięto drugi egzemplarz `toggleTheme` + `updateThemeIcon` (~linie 2574-2598) oraz zduplikowane aliasy `calculateTask4Local`/`calculateTask8Local` po bloku ENGINE.
- **N-1 (Sprzeczne klasy RIS)**: Tabela `nuts2EIS` w eksporcie CSV zaktualizowana do wartości ze snapshotu `data/external_benchmarks_snapshot.json` v1.0.0 (źródło prawdy). Naprawiono 7 rozbieżności, m.in. mazowieckie `Leader→Strong`, dolnośląskie `Strong→Moderate`, zachodniopomorskie `Moderate→Emerging`.
- **N-3 (Kolumna Instytucja)**: `p.instytucja` → `p.institution || p.instytucja` — silnik zapisuje pole jako `institution`, co eliminowało fałszywy fallback.
- **N-4 (renderPCA z flagą za mało danych)**: `renderPCA()` reaguje teraz na `pca.insufficientData === true` — wyświetla czytelny baner ostrzegawczy zamiast pustych/mylących tabel.
- **N-5 (Etykieta modelu GPQI)**: Pod suwakami symulatora GPQI (Task 8) dodano etykietę `⚠️ [Model poglądowy] — wyniki są przybliżonym oszacowaniem`.
- `npm run sync` + `npm test` **100% GREEN [PASS]**; `verify_known_issues.js` **0 błędów [PASS]**.

- **BLOKER (Code.gs:342)**: Przywrócono brakujące 3 linie w `clearAllSheets()` (` appendRow([ ... ])`, klamrę zamykającą, reset `Logs_History` oraz nagłówek JSDoc). Przechodzi `node --check` i nie powoduje już awarii całego serwera GAS.
- **Wysokie (Struktura HTML index.html)**: Usunięto 17 nadmiarowych zamykających tagów `</div>` i 1 nadmiarowy `</button>` stanowiących osierocone fragmenty po wycięciu bloków "KREATOR ARTYKUŁÓW" w zakładkach Task 4, Task 8, Task 11 oraz Task 14. Panele JASP powróciły do swoich kontenerów zakładek, usunięto sieroty przycisków `btn-dl-docx-*`.
- **Fabrykacja danych / Auto-recommender**: Usunięto zmyślone wartości `75`/`80` z `runAutoRecommender()`. W przypadku braku ocen wyświetlana jest wartość "b.d.". Poprawiono dopasowanie wielkości liter słowa kluczowego programu (`FENIKS`).
- **Filtrowanie tabeli inspektora (Task 8)**: Poprawiono indeks filtrowanej komórki z `cells[7]` na `cells[8]` w `filterInspectTable()`, eliminując mylenie GPQI Total z Regional Impact.
- **Sprawdzanie składni w testach**: Dodano weryfikację składni pliku `src/gas/Code.gs` w `test/run_tests.js` oraz flagę `--check` w `tools/sync_engine.js`.
- **Kodowanie UTF-8**: Przywrócono czyste kodowanie UTF-8 (bez Mojibake) i przeprowadzono bezpieczną normalizację LF.
- `npm run sync` + `npm test` **100% GREEN [PASS]** · `verify_known_issues.js` **0 błędów [PASS]**.

## [0.2.1] - 2026-08-03
### Naprawiono / Uzupełniono (Zgodnie z RAPORT_POPRAWNOSCI_KODU_KIMI.md)
- **K-1 (Realne 6 filarów EIRRI)**: Przepięto podgląd i eksport SPSS (Task 14) na autentyczne wyliczenia filarów z `engine.js` (`potentials.economic`, `.innovative`, `.absorption`, `.implementation`, `.environmental`, `.institutional`).
- **K-4 (Realny GPQI i pod-wskaźniki Task 8)**: Zastąpiono wzory ad-hoc autentycznymi wyliczeniami `gpqi_acc`, `gpqi_adm`, `gpqi_fin`, `gpqi_imp`, `gpqi_inn`, `gpqi_reg`, `gpqi` z silnika.
- **K-2 (Wiarygodny auto-recommender)**: Zastąpiono zahardkodowane teksty dynamicznym dopasowaniem z `task8` oraz oznaczono stan bez danych jako model poglądowy.
- **K-3 (Brak fikcyjnych trendów)**: Usunięto sztuczne generowanie danych czasowych przy braku kolumny `ROK` — dodano czytelny stan pusty.
- **B-7 (Jednolita klasyfikacja `isProjectEco`)**: Ujednolicono filtrowanie w lejkach i wykresach do kanonicznej funkcji `isProjectEco`.
- **B-8 (Guardy PCA & korelacji)**: Dodano zabezpieczenia `n < 2` oraz dzielenia przez zero przy braku wariancji w `getPCA`.
- **B-10 (Koercja Number)**: Dodano wymuszenie typu numerycznego w wyliczeniach eksportu SPSS.
- **B-12 (Zabezpieczenie SNA)**: Dodano guardy `null/undefined` dla wskaźników sieciowych w `populateSnaVariables`.

## [0.2.0] - 2026-08-03
### Usunięto / Uproszczono (Zgodnie z INSTRUKCJA_PROJEKT_UPROSZCZONY.md)
- **Usunięcie Chatbota Daisy (Gemini API)**: usunięto widget bota, przyciski interakcji oraz dedykowany kod w `index.html`. Usunięto z `src/gas/Code.gs` akcję `askDaisy`, funkcję `askDaisy()`, `callGemini()` oraz odwołanie do `GEMINI_API_KEY`. Wskazania klasyfikacji zastąpiono lokalną heurystyką `heuristicClassify()`.
- **Usunięcie Kreatora Artykułów Naukowych**: usunięto podzakładki `ai` (`btn-sub-task*-ai`), panele HTML `panel-sub-task*-ai` oraz funkcje `generateResearchPaper()`, `simulatePaperText()`, `downloadPaperFile()`. Usunięto `'ai'` z funkcji przełączania podzakładek `switchSubTab()`.
- **Usunięcie Pustych Kart UI (A-4)**: usunięto nieużywane i nie-renderowane karty `chartGpqiBenchmarking` i `chartGpqiBottleneck` z przyciskami eksportu. Połączono `chartGpqiRadar` i `chartWorkflowFunnel` w spójny układ responsywny.
- **Naprawa Sygnatur Błędów Skanera (verify_known_issues.js)**:
  - **C-002**: Całkowite usunięcie nie-bezpiecznego parsowania zapytań Gemini w `Code.gs`.
  - **Ks.#17**: Dodano zwalnianie zasobów `URL.revokeObjectURL(url)` dla wszystkich generowanych plików Blob.
  - **Ks.#32**: Wdrożono deterministyczne sortowanie rankingów z drugim kryterium rozstrzygającym przy remisach.
  - Wykonano synchronizację `npm run sync`. Weryfikacja skanerem `verify_known_issues.js` zakończona wynikiem **0 błędów [PASS]**, `npm test` **100% GREEN [PASS]**.

## [0.1.0] - 2026-07-26
### Dodano
- Inicjalizacja workspace `greenstrat-workspace`.
- Utworzenie pliku `package.json` ze skryptem `test` i zależnością `xlsx`.
- Utworzenie skryptu testowego `test/run_tests.js` do weryfikacji liczby wierszy w plikach testowych Excel (`test-data/*.xlsx`).
- Z-0 — Inicjalizacja workspace i szkielet testów (DoD: weryfikacja wierszy OK, status asercji PENDING).

### Usunięto / Skorygowano
- Usunięto plik `npm.cmd` z korzenia workspace (skorygowano środowisko zgodnie z zaleceniem — rezygnacja z wrappera na rzecz systemowego `npm`).
- Przywrócono standardową instrukcję uruchamiania testów przez `npm test`.
- Z-1 — Jedno źródło prawdy obliczeń (Luka L.15): utworzenie `engine/greenstrat_engine.js`, narzędzia `tools/sync_engine.js` (`npm run sync`) oraz pełnych asercji testowych w `test/run_tests.js`. Ujednolicenie zmieniło zachowanie frontendu dla rekordów niekompletnych (przyjęto logikę serwerową jako kanoniczną); pełna naprawa obu mechanizmów klasyfikacji braków — w Z-2. Z-1 zakończony sukcesem [PASS].
- Z-2 — Braki danych nie są ekoinnowacją (Naprawa luki L.16): usunięto defaulty `: 1` oraz ścieżkę zapasową `GEMINI_CATEGORY ∈ {1,2} → eco` w całym repozytorium. Rekord bez kompletu czterech zdefiniowanych ocen operacyjnych jest klasyfikowany jako niekompletny (`isProjectComplete = false`) i nie jest ekoinnowacją. CZY_EKOINNOWACJA przestaje być podstawą klasyfikacji; pozostaje polem deklaratywnym kontrolowanym przez bramkę spójności (rozbudowa w Z-4). Klasyfikuje wyłącznie komplet czterech ocen operacyjnych. Dodano liczniki `rekordy_niekompletne` oraz 8 syntetycznych testów jednostkowych w `test/run_tests.js`. Z-2 zakończony sukcesem [PASS].
- Z-3 — Separacja trybu DEMO od trybu badawczego (Decyzja D.12): wprowadzono flagę `TRYB_DEMO` / `demoMode` (domyślnie `false` - tryb badawczy). W trybie badawczym usunięto wszystkie generowane wartości symulowane, benchmarki (EU27=76.5, V4=64.2, OECD=71.8, ris3Alignment=82) oraz sztuczne punkty czasowe. W trybie DEMO wartości symulowane noszą etykiety `"[DEMO / SYMULACJA]"`. Dodano automatyczne testy eksportu naukowego w `test/run_tests.js`. Z-3 zakończony sukcesem [PASS].
- Z-4 — Bramka Walidacyjna 2.0 (Luka 11.1.e): zaimplementowano funkcję `validateProjects(projects, opts)` z weryfikacją słownika 18 regionów (16 województw + 2 podregiony mazowieckie), ścisłą precedencją błędów E1→E2→E3→E4→E5→E6, wyłapywaniem duplikatów ID_PROJ (zarówno wewn. jak i względem `opts.existingIds`), walidacją dodatniej wartości WART_PROJ_PLN, brakiem regresji TRL oraz strukturą rekordu odrzucenia `{ wiersz, ID_PROJ, kod, powod }`. Wpięto bramkę serwerowo w `Code.gs` (linia 125 w `doPost`) oraz kliencko w `index.html` (linia 3494 w `uploadFile`). Test negatywny na zbiorze 1000 wierszy wykazał dokładnie 120 odrzuceń (E1:30, E2:30, E3:20, E4:20, E5:10, E6:10) i 880 przyjętych. Z-4 zakończony sukcesem [PASS].
- Z-5 — Stemplowanie wyników (Zasada Z.6, wersja minimalna): zaimplementowano stałą `ENGINE_VERSION = "0.5.0"` w silniku `engine/greenstrat_engine.js`, funkcję `calculateDatasetHash` opartą na algorytmie FNV-1a 32-bit z jawnym sortowaniem kluczy pól każdego rekordu przed serializacją. Hash obejmuje WSZYSTKIE pola rekordu (`Object.keys(p).sort()`), nie tylko 9 pól wymienionych w pierwotnym planie — celowe wzmocnienie czułości hasza. Dołączono strukturę `metadata` (engineVersion, timestamp, recordCount, incompleteCount, datasetHash) do wyników `calculateTask4` i `calculateTask8`. Dodano testy determinizmu i czułości oraz zbiorczą tabelę regresji Fazy A w `test/run_tests.js`. Pierwsza wersja planu Z-5 błędnie zaproponowała „Moduł Detekcji Anomalii” zamiast zatwierdzonego stemplowania wyników; skorygowano po weryfikacji zgodności z docs/BACKLOG_AGENTA.md. Z-5 zakończony sukcesem [PASS].
- INCYDENT — SyntaxError w index.html:5161 wykryty manualnie w przeglądarce po zamknięciu Fazy A; przyczyna: wisząca deklaracja `const trendsOptions = {` z niedokładnej edycji Z-3; naprawiono; dodano stały test walidacji składni klienta do siatki regresyjnej.
- Z-6 — Usunięcie mutacji i fabrykacji ocen w parserze (Luka L.18): usunięto automatyczną korektę regresji TRL (`if (trlKoniec < trlStart) trlKoniec = trlStart`) oraz logikę `isCollinear` i wymuszanie `inn=trw=ef=trsf=0` dla `isEco=0` w parserze `uploadFile()` w `index.html`. Surowe dane przekazywane są bez zniekształceń bezpośrednio do bramki `validateProjects`, co przywraca 100% wykrywalności błędów E2 (30) oraz E5 (10) w walidacji przeglądarkowej. Z-6 zakończony sukcesem [PASS].
- LUKA L.19 — Usunięcie blokowania całego importu w runGatekeeperValidation(): usunięto sprawdzenie biznesowe `TRL_KONIEC < TRL_START` oraz niespójności ekoinnowacyjnej ze starej funkcji `runGatekeeperValidation()`, które uniemożliwiały dotarcie pliku do bramki walidacyjnej Z-4 (`validateProjects`). Logika odrzucania poszczególnych wierszy (kod E5) leży wyłącznie w Gestorze Z-4, pozwalając na przyjęcie 880 poprawnych rekordów i zareportowanie 120 błędów w teście T7.
- TEST D.11 — Próg przetwarzania lokalnego: Test D.11: próg lokalny testowany na 1000 (sukces chmury, 3,3s) i 5000 (timeout 15s, fallback lokalny) — potwierdzona granica przepustowości serwera między tymi wartościami. Próg przywrócony do kanonicznej wartości 500 wierszy.
- Z-6 (Faza B) — Domknięcie L.20: Jedno źródło prawdy dla Task 11 / Task 14: przeniesiono `calculateTask11` i `calculateTask14` do kanonicznego silnika `engine/greenstrat_engine.js` wewnątrz sekcji `ENGINE`. Usunięto zduplikowaną logikę `isEcoProj` oraz nie-bramkowane funkcje lokalne z `index.html` i `Code.gs`. W trybie badawczym (`demoMode = false`): lata odczytywane są wyłącznie z surowej kolumny `ROK` (brak = `null`), a benchmarki UE (`eu27`, `v4`, `oecd`) i `ris3Alignment` zwracają `null`. W trybie DEMO wartości symulowane posiadają etykietę `[DEMO / SYMULACJA]`. Z-6 zakończony sukcesem [PASS].
- Z-7 (Faza B) — Produkt 14.2: Realny Indeks Gotowości EIRRI w 6 filarach: zastąpiono proste formuły $LQ \times 70$ w `calculateTask14` wielowymiarowym wyliczaniem 6 filarów EIRRI (gospodarczy, naukowo-innowacyjny, absorpcyjny, wdrożeniowy, środowiskowo-transformacyjny, instytucjonalny). Zaimplementowano metodę PCA (Decyzja D.2) jako domyślny skorelowany wariant ważenia, 3 warianty ważenia (`equal`, `pca`, `expert`), zakres wrażliwości (`sensitivityRange`) oraz poziom niepewności (`uncertaintyLevel`). Z-7 zakończony sukcesem [PASS].
- Z-8 (Faza B) — Produkt 11.7: System progów z rozkładów statystycznych i Rejestr Alarmów: zaimplementowano dynamiczne wyliczanie progów ostrzegawczych (`threshWarn` z $P25$/$Q1$) oraz krytycznych (`threshCrit` z $P10$/$IQR$) z rozkładów statystycznych w `calculateTask11`. Utworzono **Rejestr Alarmów** z 3 klasami kolorystycznymi (`ZIELONY`, `ŻÓŁTY`, `CZERWONY`), wskaźnikami trwałości, poziomami wiarygodności, flagami wrażliwości na efekt niskiej bazy (`baseEffect`) oraz wyliczaniem wartości wskaźników po odrzuceniu obserwacji dominującej (`valExcludingDominant`). Z-8 zakończony sukcesem [PASS].
- Z-9 (Faza B) — Produkt 11.4: Benchmark Krajowy w 5 klasach statystycznych: zaimplementowano w `calculateTask11` klasyfikację podmiotów i regionów do 5 klas benchmarkowych (*Liderzy systemowi*, *Ponadprzeciętni*, *Poziom referencyjny*, *Wymagający poprawy*, *Krytyczni*) z wyliczanych dynamicznie percentyli rozkładu ($P10$, $P25$, $P75$, $P90$). Wdrożono **Bezpiecznik Efektu Niskiej Bazy**, który blokuje przyznanie statusu Lidera przy braku dywersyfikacji ($> 30\%$ udziału 1 projektu) i koryguje klasę do Ponadprzeciętnych z uzasadnieniem naukowym. Z-9 zakończony sukcesem [PASS].
- Z-10 (Faza B) — Produkt 14.3: Typologia Regionalna i Klasteryzacja na 6 filarach EIRRI: zaimplementowano w `calculateTask14` klasteryzację 16 województw do 4 archetypów rozwoju ekoinnowacyjnego (*Liderzy Transformacji Ekoinnowacyjnej*, *Przemysłowe Regiony Absorpcyjne*, *Regiony Umiarkowanego Potencjału Rynkowego*, *Regiony Wymagające Wsparcia Strukturalnego*). Wyznaczono odległości Euklidesowe od centroidów 6D, główny atut regionalny (`dominantStrength`), główny deficyt (`dominantDeficit`) oraz rekomendowany typ interwencji polityki publicznej (`recommendedInterventionType`). Z-10 zakończony sukcesem [PASS].
- Z-11 (Faza B) — Produkt 11.8: Krajowy Deterministyczny System Wspomagania Decyzji (DSS) w modelu Human-in-the-Loop: zaimplementowano w `calculateTask11` drzewo decyzyjne generujące pakiety rekomendacji interwencyjnych dla programów i instrumentów. Każdy pakiet zawiera max 3 warianty akcji (Wariant Główny, Optymalizacyjny, Audytowy), przewidywany wpływ, analizę ryzyk, flagę oceny prawnej (`requiresLegalReview`) oraz status rygoru ekspertalnego (`hitlStatus: "AUTOMATYCZNA"` / `"OCZEKUJĄCA_NA_WERYFIKACJĘ"`). Wdrożono bezpiecznik blokujący rekomendowanie bezpośredniego skalowania budżetu przy braku dywersyfikacji beneficjentów (`baseEffect`). Z-11 zakończony sukcesem [PASS].
- Z-12 (Faza B) — Produkt 14.7: Regionalny System Wspomagania Decyzji (Regionalny DSS) z Bezpiecznikiem i Wariantami Fallback: zaimplementowano w `calculateTask14` generator rekomendacji strategicznych dla 16 województw zasilany 6 filarami EIRRI oraz klastrami archetypowymi. Wdrożono **Bezpiecznik Niepewności**, który w przypadku wysokiego rozrzutu wariantów ważenia (`uncertaintyLevel === 'WYSOKI'`) blokuje drastyczne przesunięcia środków i wdraża wariant bezpieczny (**fallback**) wraz z rygorem akceptacji eksperckiej (`hitlStatus: "AUTOMATYCZNA"`). Z-12 zakończony sukcesem [PASS].
- Z-13 (Faza B) — Produkt 11.1: Moduł Wieloformatowego Eksportu Wyników (BigQuery, DuckDB, JSON-Stat v2.0, CSV): zaimplementowano wieloformatowy eksport w `engine/greenstrat_engine.js` (`exportToBigQueryNDJSON`, `exportToBigQueryDDL`, `exportToDuckDBDDL`, `exportToJSONStatV2`, `exportToCsv`). Wyniki gotowe do natychmiastowego zasilania chmurowych hurtowni BigQuery i pulpitów Google Looker Studio. Z-13 zakończony sukcesem [PASS].
- Z-14 (Faza B) — Produkt 14.8: Model EKO_Lokacji i Katalog 17 Instrumentów: zaimplementowano w `calculateTask14` model rozmieszczenia węzłów i hubów innowacyjnych oraz dedykowany katalog 17 instrumentów wsparcia regionalnego. Z-14 zakończony sukcesem [PASS].
- Z-15 (Faza B) — Produkty 14.6 & 14.9: 10 Warstw Mapowych i Katalog Rekomendacji JST: zaimplementowano w `calculateTask14` 10 kanonicznych warstw przestrzennych oraz interaktywny katalog rekomendacji dla Jednostek Samorządu Terytorialnego. Z-15 zakończony sukcesem [PASS].
- Z-16 (Faza B) — Produkty 11.5 & 14.5: Importer ETL & Benchmark Międzynarodowy (Eurostat/EIS/GUS BDL): utworzono moduł importera `tools/import_external_datasets.js` oraz zbiór `data/external_benchmarks_snapshot.json` z 22 wskaźnikami empirycznymi dla 16 województw NUTS 2 i Polski na tle UE-27 / V4 / OECD. Powiązano z `calculateTask11` i `calculateTask14`. Z-16 zakończony sukcesem [PASS].
- KOREKTA UI & PEŁNOŚĆ WSKAŹNIKÓW: Poprawiono układ podmenu zadań (zastosowano `sticky top-0 z-40`), uaktywniono przyciski Przewodnika Krok po Kroku (Stepper 1–5), dodano przycisk przełącznika widoczności benchmarku (`⚡ Przełącz Widoczność Modułu`) oraz wdrożono empiryczne dane UPRP / GUS BDL w Baza 1–6.
- DOKUMENTACJA: Utworzono kompletną Instrukcję Obsługi Systemu `docs/INSTRUKCJA_OBSLUGI_GREENSTRAT.md` oraz uaktualniono kontekst projektu `GREENSTRAT_PROJECT_CONTEXT.md`.
- Z-13 (Faza B) — Moduł Wieloformatowego Eksportu Danych (Produkt 11.1 / Google BigQuery, Parquet, DuckDB, JSON-Stat v2.0, CSV): wdrożono `tools/export_formats.js` oraz konwertery pomocnicze w silniku. Moduł umożliwia bezstratny eksport wyliczonych zbiorów danych i wskaźników do 5 formatów: Google BigQuery (schemat GoogleSQL DDL + NDJSON dla ładunków chmurowych i Google Looker Studio), Apache Parquet, DuckDB, JSON-Stat v2.0 (standard Eurostat/GUS) oraz CSV (z UTF-8 BOM). Z-13 zakończony sukcesem [PASS].
- Z-14 (Faza B) — Produkt 14.8: Model EKO_Lokacji i Katalog 17 Bazowych Instrumentów Wsparcia: zaimplementowano w `calculateTask14` wyliczanie macierzy EKO_Lokacji per województwo oraz słownik 17 bazowych typów interwencji (zgodnie ze specyfikacją `docs/Odpowiedzi na pytania.docx.txt`). Wdrożono **Bezpiecznik Niestabilności**, który dla regionów o wysokiej niepewności lub efektach niskiej bazy automatycznie blokuje rekomendowanie stacjonarnego centrum i wdraża **`Model Sieciowy / Mobilny (Pilotażowy)`** ze współdzieleniem infrastruktury i rygorem `hitlStatus: "AUTOMATYCZNA"`. Z-14 zakończony sukcesem [PASS].
- Z-15 (Faza B) — Produkty 14.6 & 14.9: Mapy Specjalizacji, Ryzyka i Białych Plam (10 Warstw Mapowych) oraz Katalog Rekomendacji dla JST i Województw: zaimplementowano w `calculateTask14` wyliczanie 10 kanonicznych warstw mapowych (`regionalMaps`) oraz ustandaryzowanego katalogu rekomendacji dla samorządów JST i organów wojewódzkich (`jstRecommendations`), łączącego 17 bazowych instrumentów, horyzonty czasowe, wskaźniki KPI i status `hitlStatus: "AUTOMATYCZNA"`. Z-15 zakończony sukcesem [PASS].
- Z-16 (Faza B) — Produkty 11.5 & 14.5: Importer ETL Danych Zewnętrznych oraz Benchmark Międzynarodowy (Eurostat / European Innovation Scoreboard / Eco-Innovation Index / GUS BDL / UPRP): stworzono moduł ETL `tools/import_external_datasets.js` oraz zwalidowany statyczny snapshot `data/external_benchmarks_snapshot.json` (22 wskaźniki dla 6 filarów per NUTS 2). Zasilono karty *"Polska na tle UE"* (w `calculateTask11`) oraz europejskie benchmarki regionów (w `calculateTask14` — regiony bliźniacze i aspiracyjne). Z-16 zakończony sukcesem [PASS] — Faza B domknięta w 100%!

## [A-1] - 2026-08-03
### Naprawiono
- **A-1 (krytyczne, Produkt 11.5) — usunięcie fabrykowanego benchmarku międzynarodowego z trybu badawczego.** Audyt UI wykazał, że w trybie badawczym karta „Polska na tle UE" pokazywała wartość wyliczoną wzorem `clamp(EISPI × 0.724, 50, 95)` opatrzoną etykietą `source: "GUS BDL / Eurostat 2024 Snapshot"` i bez etykiety `[DEMO / SYMULACJA]` — na pełnej bazie 31 301 rekordów dawało to indeks **52,9** zamiast rzeczywistych **72,4**. Przyczyną było to, że klient i Google Apps Script nie mają dostępu do modułów Node ani do systemu plików, więc gałąź czytająca `data/external_benchmarks_snapshot.json` była w przeglądarce kodem martwym, a jedyną osiągalną ścieżką pozostawała gałąź `useExternalBenchmark` ze wzorem. **Zmiany:** (1) snapshot wbudowano do bloku `ENGINE` jako stałą `EXTERNAL_BENCHMARKS_SNAPSHOT` (`engine/greenstrat_engine.js`), skąd `npm run sync` roznosi go do `index.html` i `Code.gs`; (2) usunięto gałąź `useExternalBenchmark` — `calculateTask11` czyta teraz `options.externalSnapshot`, a w razie jego braku wbudowaną stałą; (3) z kontraktu wyjścia usunięto pola `v4` i `oecd`, które nie miały pokrycia w snapshocie (wartość `85.0` nie występuje w nim w ogóle), a wynik wzbogacono o `indicators`, `snapshotVersion` i `snapshotGeneratedAt`; (4) usunięto martwy obiekt `benchmark` (przypisywany, nigdy nieodczytywany) z kolejnym kompletem literałów `eu27`/`v4`/`oecd`; (5) w `index.html` przebudowano wykres benchmarku — zamiast 4 słupków z zaszytymi wartościami zastępczymi `benchmark.v4 || 76.5` i `benchmark.oecd || 85.0` pokazuje 5 słupków wyłącznie ze snapshotu (Polska, UE-27, Czechy, Słowacja, Węgry) i nie podstawia już EISPI pod indeks EIS; (6) eksport CSV benchmarku zwraca `b.d.` zamiast literałów zastępczych i podaje źródło z wersją snapshotu.
### Dodano
- **Test regresji ścieżki produkcyjnej (A-1).** Audyt wykazał, że asercja Z-16 sprawdzała moduł ETL `tools/import_external_datasets.js`, a nie `calculateTask11` — ścieżka, którą realnie wykonuje aplikacja, nie była pokryta żadnym testem, dlatego fabrykacja przechodziła przez zieloną siatkę. Dodano w `test/run_tests.js` test wywołujący silnik dokładnie tak jak klient (`{ demoMode: false, useExternalBenchmark: true }`, bez `externalSnapshot`) i asertujący, że indeks pochodzi ze snapshotu, a **nie** równa się wartości ze wzoru, oraz że pola `v4`/`oecd` są nieobecne.
- **Test rozjazdu kopii snapshotu (A-1).** Snapshot istnieje w dwóch miejscach — pliku JSON (źródło dla ETL/Node) i stałej w bloku `ENGINE` (jedyna kopia dostępna dla przeglądarki i GAS). Dodano asercję równości obu kopii, żeby nie dało się zaktualizować jednej strony bez drugiej. Stałą wyeksportowano w `module.exports` silnika.
### Zmieniono
- Asercja Z-6 w `test/run_tests.js` wymagała `task11.benchmark === null` w trybie badawczym — reguła obowiązująca, dopóki nie istniało realne źródło danych. Po wbudowaniu snapshotu zastąpiono ją mocniejszym warunkiem: benchmark musi pochodzić ze snapshotu (zgodność `summaryInnovationIndex`, `distanceToEuAverage` i `snapshotVersion` z plikiem źródłowym) oraz nie może zawierać pól `v4`/`oecd`. Wartości wyroczni `test/fixtures_kontrolne.json` pozostały nienaruszone.
- Weryfikacja: `npm test` [PASS], hasze wyroczni bez zmian (`e69d1ad3`, `a41fae75`, `32dd9923`, `17d343dc`), EIFII/ISBI/CRI bez zmian. W aplikacji na 31 301 rekordach: indeks 72,4, dystans −27,6, wykres 5 słupków (72,4 / 100,0 / 91,2 / 68,5 / 69,8), 29 przejść nawigacyjnych bez błędu runtime.

## [Audyt UI] - 2026-08-03
### Dodano
- `audit/RAPORT_AUDYTU_UI_PELNA_BAZA_31301.md` — raport z audytu interfejsu przeprowadzonego w przeglądarce na pełnym zbiorze `GREENSTRAT_test_pelna_baza_31301.xlsx` (31 301 rekordów, tryb badawczy, silnik 0.5.0). Wynik: 29 przejść nawigacyjnych, **0 błędów runtime**, bramka Z-4 31 301/0, wskaźniki EIFII/ISBI/CRI zgodne z wyrocznią co do cyfry, 29 wykresów wyrenderowanych, wszystkie produkty Fazy B wpięte w UI. Zidentyfikowano 4 znaleziska merytoryczne: **A-1 (krytyczne)** — Produkt 11.5 prezentuje w trybie badawczym wartość wyliczoną wzorem `clamp(EISPI × 0.724, 50, 95)` pod etykietą źródła „GUS BDL / Eurostat", bo klient wywołuje wyłącznie gałąź `useExternalBenchmark`, a gałąź `externalSnapshot` jest w przeglądarce kodem martwym (snapshot czytany jest przez moduł Node); test Z-16 sprawdza moduł ETL zamiast `calculateTask11`, więc ścieżka produkcyjna jest niepokryta. **A-2** — `datasetHash` nieporównywalny między aplikacją (`debee221`) a wyrocznią (`17d343dc`), bo parser UI dokłada wyliczone pole `DELTA_TRL` przed stemplowaniem; algorytm FNV-1a potwierdzony jako spójny środowiskowo (błąd #23 Księgi pozostaje zamknięty). **A-3** — przy braku kolumny `ROK` zwracana jest oś czasu 2021–2027 z zerami zamiast komunikatu „BRAK DANYCH CZASOWYCH" (konflikt z ustaleniem Z-6). **A-4** — `chartGpqiBenchmarking` i `chartGpqiBottleneck` nie mają kodu renderującego i pozostają trwale puste. Zgodnie z zasadami #2 (STOP-AND-ASK) i #6 (formuły zamrożone) nie wprowadzono żadnych zmian w kodzie — raport zamyka fazę diagnostyczną i czeka na decyzje HITL.

## [Higiena repozytorium] - 2026-08-03
### Usunięto
- Usunięto zdezaktualizowany duplikat `index.html` z korzenia workspace. Plik był kopią frontendu dodaną w commicie `fc01a06` na potrzeby wdrożenia Netlify, ale rozjechał się z kanonicznym `src/gas/index.html` o 505 linii — nie otrzymał poprawek z commitów `886e629` (usunięcie linii poza zasięgiem w `DOMContentLoaded`) ani `e9834ee` (zabezpieczenia `null` przed `toString()` dla `ABSORPCJA` i `STATUS_*`). Duplikat nie był podłączony do żadnego mechanizmu: `netlify.toml` publikuje katalog `src/gas`, `tools/sync_engine.js` ma tylko dwa cele (`src/gas/Code.gs`, `src/gas/index.html`), a `test/run_tests.js` waliduje składnię wyłącznie kanonicznego pliku. Jedyne źródło prawdy frontendu to `src/gas/index.html`. Regresja po usunięciu: `npm test` [PASS] (hasze wyroczni bez zmian: `e69d1ad3`, `a41fae75`, `32dd9923`, `17d343dc`).














